# Work Log

## 2026-06-16

- Created and hardened the product design spec for deterministic Warhammer 40k terrain/deployment extraction, base-aware 2D LOS, heatmaps, exposure analysis, and local GUI workflows.
- Ran adversarial reviewer subagents for extraction/PDF-CV, LOS/math, GUI trust workflow, and verification/reproducibility; integrated their findings into the spec.
- Added infrastructure requirements for a public-ready repository, source-PDF policy, CI, and tooling.
- Created an implementation plan for project infrastructure.
- Set the end-to-end product goal: deterministic official-PDF extraction, canonical validation, base-aware LOS, GUI workflows, browser UX tests, security/code review, and independent reviewer approval.
- Pivoted the infrastructure from Node/Vite to a Python-only FastAPI-served GUI after deciding the project does not need Node for the first release.
- Verified the Python-only checkpoint with Ruff, mypy, pytest, and whitespace checks.
- Ran consultant subagents for extraction/data spine, LOS/math, and UX/release evidence. Key adjustment: implement a synthetic canonical data spine and fixture-first GUI before official-PDF extraction.
- Split verification intent into public, GUI, and full-local gates so public CI stays copyright-safe while final release can require the pinned official-PDF cache.
- Implemented the first deterministic data-spine slice: canonical layout models, source manifest models, stable canonical JSON and layout hashing, schema export, synthetic layout fixture, public-safe official source manifest, and FastAPI endpoints for layouts and source status.
- Verified the data-spine checkpoint with `scripts\verify.cmd`: Ruff, mypy, and 17 pytest tests passed.
- Ran a targeted adversarial data-spine reviewer. The reviewer blocked LOS work until canonical inputs were hardened against non-finite coordinates, invalid polygons, blockers outside footprints, unsafe manifest paths/URLs/hashes, duplicate validation records, mutable models, and unpinned schema versions.
- Fixed the accepted data-spine review findings with regression tests. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 42 pytest tests passed.
- Implemented the first LOS slice: point LOS blocker intersection, base-center legality, deterministic disk-sampled base-aware LOS, and `/api/layouts/{layout_id}/los`.
- Verified the LOS checkpoint with `scripts\verify.cmd`: Ruff, mypy, and 54 pytest tests passed.
- Implemented point-sampled firing-lane heatmaps, movement exposure, terrain coverage delta metrics, and FastAPI endpoints for `/heatmap`, `/exposure`, and `/terrain/{feature_id}/coverage`.
- Verified the analysis checkpoint with `scripts\verify.cmd`: Ruff, mypy, and 60 pytest tests passed.
- Ran targeted adversarial LOS review. The reviewer blocked GUI work until endpoint contact semantics, movement-blocking terrain flags, base sampling metadata, analysis request validation, richer heatmap/exposure cell outputs, and unknown feature/deployment errors were fixed.
- Fixed the accepted LOS review findings and regenerated schemas/fixtures after canonical model changes. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 69 pytest tests passed.
- Ran LOS re-review. The reviewer found non-standard JSON numeric tokens could still cause 500 responses or bypass optional field validation.
- Fixed API-boundary non-finite numeric handling with finite request fields and sanitized validation errors. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 72 pytest tests passed.
- Added the Warhammer 40,000 Event Companion PDF to the public-safe official source manifest after the user noted it contains map layouts. The PDF remains only in the gitignored local cache; the committed manifest stores its URL and SHA-256 hash.
- Built the first fixture-backed local GUI with layout/source status, validation status, canvas board rendering, click-on-map base-aware LOS, heatmap, deployment exposure, terrain coverage, and export controls.
- Added an API regression for illegal base centers so base-aware LOS returns 422 instead of surfacing a server error.
- Verified the GUI/API checkpoint with `scripts\verify.cmd`: Ruff, mypy, and 74 pytest tests passed.
- Browser-tested the fixture-backed GUI on `http://127.0.0.1:8000`: dashboard loaded, all three source PDFs showed `hash_match`, map clicks returned `disk-sample-v1` LOS with 289 samples, heatmap/exposure/terrain/export controls returned JSON, mobile-width layout had no horizontal overflow, and there were no browser console errors.
- Updated the dev launcher to default to `127.0.0.1:8765` and support `FORTYK_LOS_HOST` / `FORTYK_LOS_PORT` overrides so parallel Codex threads can avoid local port collisions.
- Ran an independent adversarial GUI/API review. The reviewer blocked approval on uncaught API validation errors in click-on-map LOS, stale browser-evidence test filenames, and `innerHTML` use for API-derived text.
- Fixed the reviewer findings with panel-level async error handling, explicit `textContent` DOM construction, browser-evidence path validation, and corrected evidence file references. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 78 pytest tests passed.
- Browser-tested the reviewer regression on `http://127.0.0.1:8765`: an illegal base-center click rendered `Error: source base center is not legal`, emitted no error-level console logs, and a subsequent legal click still returned `disk-sample-v1` LOS with 289 samples.
- Re-ran independent GUI/API re-review after the hardening fix; the reviewer approved the fixture GUI/API slice. The current local full verifier passed afterward with Ruff, mypy, and 80 pytest tests.
- Implemented the first Event Companion vector extraction slice: deterministic board transform, deployment-zone extraction, terrain placement extraction from gray vector drawings, terrain-label proximity, and validation records noting pending official footprint/wall enrichment. Synthetic vector-PDF and cached official page 9 tests pass.
- Ran an independent adversarial extraction review. The reviewer blocked approval until official page 9 had a stronger hash-pinned geometry regression, measurement-annotation scope was honest, proxy perimeter blockers were removed, label uncertainty was visible, and analysis was blocked for unreviewed extracted layouts.
- Hardened the extraction slice: official page 9 now checks the pinned Event Companion SHA-256, exact terrain count, deployment bounds, feature labels/bounds, no emitted LOS blockers, deployment-depth measurement annotation cross-check, label-review warning, terrain-measurement pending warning, and API 409 readiness blocking for unresolved extracted geometry. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 82 pytest tests passed.
- Added GUI trust metadata for layouts. The selector now marks extracted warning-state layouts, and the side panel shows validation status, source document, page, and extraction method. Browser evidence on `http://127.0.0.1:18766` confirmed Event Companion page 9 rendered as `Layout A (extracted warning)` with no console errors.
- Added deterministic terrain-footprint outline extraction from the official Terrain Area Footprints PDF. The extractor selects the five large green vector outlines from the hash-pinned local cache and records page, bounds, path-command count, and point count; this is still outline evidence only, not LOS-ready wall geometry. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 85 pytest tests passed.
- Ran independent review for the terrain-footprint outline extraction checkpoint. The reviewer approved the outline-only scope but required exact official point-count regression coverage; the follow-up pins point counts, adds deterministic candidate tie-breakers, documents the area threshold calibration, and passes `scripts\verify.cmd` with Ruff, mypy, and 85 pytest tests.
- Added `/api/extraction/terrain-footprints` and a GUI Footprints panel so hash-matched official terrain-outline evidence is inspectable without treating it as LOS-ready blocker geometry. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 87 pytest tests. Browser evidence on isolated port `18767` confirmed `hash_match`, `terrain-footprint-vector-v1`, five outline rows, no horizontal overflow, and no error-level console logs; the temporary server was stopped afterward.
- Ran independent review for the terrain-footprint evidence endpoint. The reviewer required direct API regressions for missing and hash-mismatched cache states; added tests that monkeypatch the extractor to fail if called and confirmed both states return empty outlines without parsing untrusted PDFs. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 89 pytest tests.
- Implemented terrain-footprint template evidence and provisional placement matching. Templates normalize outline points plus assigned green fragment paths, official regression pins five template aspect ratios and fragment counts, matching uses deterministic aspect scoring with ambiguity/weak-label review states, and Event Companion extraction with templates adds review-required validation records while still emitting no blockers. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 94 pytest tests.
- Ran independent review for provisional footprint template matching. The reviewer required full-fragment containment, ordered path evidence instead of globally deduped paths, and invalid aspect-ratio filtering; the follow-up adds regressions for those cases, direct weak/fallback/slash review-state tests, and an official normalized-evidence digest. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 101 pytest tests.
- Added `/api/layouts/{layout_id}/footprint-matches` and a GUI Matches panel for per-feature provisional footprint match evidence. Hash-mismatched terrain templates are not parsed, missing layouts return 404, extracted layout details include match review warnings when templates are available, and official layouts remain blocked for LOS. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 105 pytest tests. Browser evidence on isolated port `18768` confirmed Event Companion page 9 shows 16 provisional match rows and the match review warning, with no console errors; the temporary server was stopped afterward.
- Ran independent review for the exposed footprint match evidence slice. The follow-up renames the GUI panel to `Provisional Matches`, renders match scores, memoizes terrain-footprint templates per repository instance, and adds a regression proving match evidence still leaves extracted-layout LOS blocked. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 106 pytest tests.
- Ran a Codex Security diff scan with three targeted read-only reviewer subagents. The scan found two issues: unbounded analysis work from tiny positive grid steps and an unused `httpx2` dependency in the install trust base. Fixed both with request-size caps, 422 API handling, regression tests, and dependency/lock cleanup. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 112 pytest tests.
- Wrote the security scan bundle under `.tmp/security-scans/wh40k-los-analyzer/1d500bd_2026-06-16T144238`, including work ledgers, candidate ledgers, validated markdown report, and rendered HTML report. Public GitHub setup is blocked because the local `gh` token for `kraken-yoba` is invalid and the GitHub connector currently times out during startup.
- Ran final independent extraction/LOS and UX reviewers. Both passed the current accepted scope. Fixed the extraction/LOS reviewer's non-blocking hardening note by returning HTTP 422 for point-only LOS points outside the board; verification passed with `scripts\verify.cmd`: Ruff, mypy, and 113 pytest tests.
- Added deterministic provisional wall/blocker generation from matched Terrain Area Footprints fragment paths. Template matches now record whether reciprocal aspect matching rotated the template 90 degrees, fragment points are transformed into board inches and clamped inside each terrain footprint, and Event Companion page 9 emits 376 provisional wall segments while remaining warning-state and blocked for analysis until review. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 116 pytest tests.
- Added an explicit layout Load button so Browser automation and users can deterministically apply a selected layout even when native select change handling is unreliable in hidden browser sessions. Browser evidence on isolated port `18770` confirmed page 9 loads through the button, shows blocker review warnings, keeps official map-click LOS blocked, has no console errors, and the temporary server was stopped afterward.
- Incorporated terrain-category feedback by extracting Dense/Light/Unknown category markers from the Event Companion vector maps and generating provisional official wall candidates only for Dense terrain features. The GUI now renders terrain categories with distinct fills and review-gated footprint blockers as dashed amber overlays rather than final red walls. Terrain Area Footprints cubic Bezier fragments are tessellated into deterministic samples instead of using raw control points as wall vertices. Focused verification passed with 77 pytest tests before full verification.
- Verified the dense-only blocker/category slice with `scripts\verify.cmd`: Ruff, mypy, and 121 pytest tests passed. Browser evidence on isolated port `18771` confirmed Event Companion page 9 loads, shows 16 provisional matches and blocker-review warnings, emits 386 provisional blockers restricted to the six Dense terrain features, keeps click-on-map LOS blocked, has no console errors, and has no horizontal overflow at 390px mobile width.
- Ran an independent adversarial review of the dense-only blocker/category slice. The reviewer found no blocking or correctness issues; the follow-up added an explicit regression proving Dense category markers take precedence when Dense and Light markers overlap the same terrain feature. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 122 pytest tests passed.
- Added deterministic Event Companion visual sanity reports. The new report renders the source PDF page, segments raster evidence for board, deployment zones, and terrain features, compares those shapes against extracted canonical geometry with board-inch residuals, and exposes advisory vision status as `not_run` until a model verifier is credential-gated. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 128 pytest tests passed. Browser evidence on isolated port `18772` confirmed page 9 Sanity status `passed`, board/deployment/terrain checks `1/1`, `2/2`, and `16/16`, no console errors, and no mobile horizontal overflow.
- Addressed visual-sanity review feedback by isolating the advisory Sanity panel fetch from canonical layout loading. A failed visual-sanity endpoint can now only render an error in the Sanity panel and cannot block board, validation, or match rendering.
- Added the first accepted-warning workflow. Validation warnings can be accepted in the local GUI after a current layout-hash check, accepted layouts still show warning/degraded state, and strategy analysis responses include `validation_state: accepted_with_warnings`; browser evidence on isolated port `18776` confirmed page 9 warning acceptance followed by a successful degraded Heatmap run.
- Added feature provenance inspection. The GUI now has explicit LOS/Inspect modes; Inspect clicks select terrain features, highlight the selected footprint, and show feature id, label, terrain category, blocker count, source document/page/method, and warning review state. Browser evidence on isolated port `18778` confirmed Event Companion page 9 `terrain-01` provenance without changing LOS point state.
- Clarified terrain semantics in the GUI after user feedback that red wall lines could be misread as generic terrain. The side panel now shows Dense, Light, Exposed, Unknown, and Dense wall candidate counts from the loaded layout, while the backend remains dense-only for provisional blockers. Browser evidence on isolated port `18783` confirmed Event Companion page 9 shows `Dense 6`, `Exposed 0`, `Unknown 10`, and `Dense wall candidates 386`, with no desktop/mobile overflow or console errors.
- Added a source PDF underlay workflow. Hash-matched Event Companion layouts can now serve an exact-size board-cropped PNG under `/api/layouts/{layout_id}/source-underlay.png`, and the GUI has a Source underlay toggle that draws that crop below extracted terrain/blocker overlays with stale-layout and stale-toggle response guards. Browser evidence on isolated port `18787` confirmed page 9 showed `Underlay visible`, returned to `Underlay off` when disabled, the PNG endpoint returned valid `880x1200` image bytes, and desktop/mobile layouts had no overflow or console errors.
- Added Core Rules terrain-semantics evidence. `/api/rules/terrain-semantics` now hash-checks the local Core Rules PDF, verifies short section anchors, and exposes paraphrased LOS implications for visibility, terrain categories, Dense/Solid wall candidates, Light/Exposed non-blockers, and future 3D-aware LOS. Browser evidence on isolated port `49231` confirmed `hash_match`, `backed`, expected page anchors `24/46/48/50`, no desktop/mobile overflow, and no console errors.
- Addressed adversarial review findings for Core Rules semantics. Rule anchors now require body terms after the section heading so TOC-only pages cannot produce `backed` evidence, duplicate rules sources return `source_ambiguous`, and the GUI Rules fetch is isolated from the layout/source/footprint initialization barrier. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 154 pytest tests passed.

## 2026-06-17

- Added a hash-pinned official source downloader. `fortyk_los_backend.domain.source_cache` skips already verified cache files, downloads missing or mismatched files through the manifest-approved URLs, verifies SHA-256 before replacing cache files, preserves existing cache files when a new download fails verification, and removes temporary `.part` files on mismatch. The new `scripts\download-sources.cmd` wrapper runs `python -m fortyk_los_backend.download_sources` through the project uv environment. Focused TDD verification passed with 9 downloader/CLI pytest tests, and full verification passed with `scripts\verify.cmd`: Ruff, mypy, and 163 pytest tests.
- Hardened the source downloader after adversarial review. The CLI now streams responses directly to `.part` files with a 100 MiB cap, revalidates redirect targets before reading, removes partial files on fetch failures, and reports bad document ids without a traceback. Focused downloader/CLI verification passed with 14 pytest tests; full verification passed with `scripts\verify.cmd`: Ruff, mypy, and 168 pytest tests.
- Addressed the source downloader re-review note by moving missing/invalid manifest handling inside the concise CLI error boundary. Focused downloader/CLI verification passed with 16 pytest tests; full verification passed with `scripts\verify.cmd`: Ruff, mypy, and 170 pytest tests.
- Implemented the snapped official-footprint product loop. Event Companion terrain placement candidates now snap to the 1-inch board grid, overlapping snapped candidates are automatically removed, snapped edge offsets are cross-checked against printed inch annotations when present, deterministic warnings are auto-processed, and the GUI no longer exposes manual warning-accept buttons. Candidate footprint matches can emit solid Dense wall fragments; ambiguous `needs_review` matches do not emit wall blockers and keep official-layout analysis blocked until a future deterministic disambiguation pass resolves them. Verification passed with `scripts\verify.cmd`: Ruff, mypy, and 171 pytest tests before the follow-up reviewer hardening.
- Hardened the snapped-footprint loop after adversarial review. Wall blocker generation now skips `needs_review` footprint matches, aggregate match warnings are accepted only when all matches are deterministic candidates, printed terrain measurement mismatches become unresolved warnings, high snap residuals without measurement evidence require review, and extracted layout selector labels include source page numbers so page 9 can be selected intentionally.
- Added terrain footprint normalization evidence. The new report extracts canonical per-layout footprint size options from snapped terrain geometry, detects terrain footprint elements from the rendered Event Companion image with deterministic CV, matches image elements to closest size options with rotation allowed, and exposes the evidence through `/api/layouts/{layout_id}/footprint-normalization` plus a GUI panel. Follow-up browser testing found and fixed gray-context false positives by measuring raster evidence inside each expected snapped footprint window; reviewer hardening now collapses rotated size options, includes source provenance and feature-linked match reasons/deltas, and pins an optional official page-9 regression. Added `/api/layouts/{layout_id}/terrain-symmetry` as an advisory 180 degree rotational symmetry check, and made technical evidence panels collapsible by default. Full verification passed with `scripts\verify.cmd`: Ruff, mypy, and 193 pytest tests.
- Implemented the symmetry-aware reconciliation loop. `/api/layouts/{layout_id}/terrain-reconciliation` now reports source footprint catalog evidence, map extraction, grid snap, positioned corner-measurement verification, maximum-cardinality one-to-one symmetry matching, viable self-center and mirror-partner candidate alternatives, final measurement/symmetry statuses, and unresolved warning codes. The GUI adds collapsible Process and Reconciliation evidence panels, labels the source-footprint-catalog step separately from map extraction, clears stale process evidence on fetch failure, and avoids presenting viable candidates as applied geometry. Reviewer hardening filters source-catalog dimensions and Dense wall fragment evidence to deterministic `candidate` footprint-template matches only; `needs_review` template matches stay advisory. Full verification passed with `scripts\verify.cmd`: Ruff, mypy, and 208 pytest tests.
